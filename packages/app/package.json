{
  "name": "@wowarenalogs/app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:app": "shx rm -rf dist && tsc -p . && webpack --config webpack.preload.config.js",
    "release:app": "git checkout release/app && git pull && git merge --no-edit --no-ff main && git push && git checkout main"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.182",
    "@types/node": "^17.0.23",
    "@types/node-fetch": "^2.6.2",
    "shx": "^0.3.4",
    "webpack": "^5.72.0",
    "webpack-cli": "^4.9.2"
  },
  "dependencies": {
    "@wowarenalogs/parser": "^4.6.4",
    "@wowarenalogs/shared": "^1.0.0",
    "electron": "17.1.0",
    "electron-devtools-installer": "^3.2.0",
    "fs-extra": "^10.1.0",
    "lodash": "^4.17.21",
    "moment": "^2.29.1",
    "node-fetch": "^2.6.7",
    "update-electron-app": "^2.0.1"
  }
}
